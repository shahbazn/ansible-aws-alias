---
- name: Simple Create
  hosts: localhost
  vars:
    aws_account_alias: "test-alias-12345"
    aws_account_state: present

  pre_tasks:
    - name: Clean existing account alias
      raw: /usr/bin/python ../clean_alias.py

  roles:
     - aws_alias_role